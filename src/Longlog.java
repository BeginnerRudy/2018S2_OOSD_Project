import org.newdawn.slick.SlickException;

public class Longlog extends RideableVehicle{
    public Longlog(String imageSrc, float x, float y, float speed, boolean isMoveToRight) throws SlickException {
        super(imageSrc, x, y, speed, isMoveToRight);
    }
}
