import org.newdawn.slick.SlickException;

public class Bus extends KillableVehicle{
    public Bus(String imageSrc, float x, float y, float speed, boolean isMoveToRight){
        super(imageSrc, x, y, speed, isMoveToRight);
    }
}
